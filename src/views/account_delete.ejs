<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <title>Donors</title>
  <link rel="stylesheet" href="https://bootswatch.com/5/lux/bootstrap.min.css">
  <link rel="stylesheet" href="/style.css" />
</head>

<body>
  <!-- navigation -->
  <%- include("partials/navbar") %>

  <h2 class="centert">Delete <%= user.email %>?</h2>

  <div class="outside">
    <div class="inside">
      <div class="card bg-light">
        <div class="card-body">
          <!-- This form delivers the data to /deleteuser. -->
          <% if(info.status == "suser") { %>
          <form action="/deleteluser/<%= user.id %>" method="POST">
            <button type="submit" class="btn btn-danger">Delete</button>
            <a href="/lusers" class="btn btn-info">Cancel</a>
          </form>
          <% } %>
          <!-- This form delivers the data to /deletesuser. -->
          <% if(info.status == "owner") { %>
          <form action="/deletesuser/<%= user.id %>" method="POST">
            <button type="submit" class="btn btn-danger">Delete</button>
            <a href="/susers" class="btn btn-info">Cancel</a>
          </form>
          <% } %>
        </div>
      </div>
    </div>
  </div>

</body>

</html>